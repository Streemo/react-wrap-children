{
  "name": "react-wrap",
  "version": "1.0.0",
  "description": "let renderThis = wrap(this.props.children, someProps)",
  "main": "./dst/wrap.js",
  "scripts": {
    "compile-src": "babel src --presets babel-preset-es2015 --out-dir dst",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jay Patel <jay@pitel.co> {http://pitel.co)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  },
  "dependencies": {
    "react": "^15.2.0",
    "typetastic": "^1.0.6"
  }
}
